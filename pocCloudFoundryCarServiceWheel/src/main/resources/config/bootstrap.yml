
spring:
  application:
    name: service-wheel
  cloud:
    config:
      fail-fast: true
      uri: ${vcap.services.configserver.credentials.uri:http://localhost:8888}
      
#      discovery: 
#        enabled: true
#        serviceId: config-server



