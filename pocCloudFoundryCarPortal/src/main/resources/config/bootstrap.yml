
spring:
  application:
    name: car-portal
  cloud:
    config:
      fail-fast: true
      uri: ${vcap.services.configserver.credentials.uri:http://localhost:8888}  
      
#      discovery: 
#        enabled: true
#        serviceId: config-server
       